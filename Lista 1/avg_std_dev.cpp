#include <iostream>

using namespace std;

int columns;
int rows;

int main(){
    cout << 'Please type the number of rows and columns of the matrix';
    cin >> rows;
    cin >> columns;
    int matrix[rows][columns];
}